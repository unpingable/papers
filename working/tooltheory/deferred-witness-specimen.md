# Deferred Witness — green specimen, RE-ASSEMBLY verdict (do NOT coin)

**Status:** green zero-axiom Lean specimen + **RE-ASSEMBLY** prior-art verdict. NOT a kernel,
NOT novel doctrine. Do not christen "Deferred Witness" / "Temporal Backflow" as a primitive.
Filed 2026-06-26. Lean: `~/git/lean/LeanProofs/Scratch/DeferredWitness.lean`.

The lawful-exception companion to the retroactive-standing refusal: when may a witness observed
*after* a claim was asserted legitimately complete it (reconciliation), vs launder it
(necromancy)? The engineering is real and compiles; the *idea* is an assembly of named parts.

## Build receipts (2026-06-26)

**Arrived red** — the dropped file had 4 errors (`omega` blind to constraints;
`decide` hitting the opaque-`Admissible` trap). Logged because arrived-red-then-fixed is a
different fact from arrived-green. The 4 were proof-tactic bugs, **zero doctrinal content**;
fixed the tactics only. Rebuilt: **EXIT=0**, no sorry/admit/axiom. The four teeth
(`necromancy_rejected`, `mutated_terms_rejected`, `no_retroactive_standing`,
`pending_not_admissible`) depend on **no axioms at all**. All `#eval`s return predicted values
(incl. the lapse path: `none` + expired → `lapsed`, never `pending`).

## What it actually contains (the engineering, which is fine)

`LawfulCompletion` predicate with named refusal surfaces, a `Backflow` violation taxonomy +
executable `firstViolation` classifier, a lifecycle with **active lapse** (`pending → lapsed`,
only `completed` admissible), and a Layer-B open frontier (kind-vs-instance scope as a
spend-down `Budget`). The recursion floor — `necromancy_rejected` re-firing when an exhausted
grant is refreshed — is the one genuinely nice structural observation, and it is just
`signed≠witnessed` aimed at the grant (corpus-owned).

## Prior-art wall — codex/gpt-5.5, web-searched primaries, 2026-06-26: RE-ASSEMBLY

| Piece | Owned by |
|---|---|
| base refusal (outcome ≠ prior authority; "needs a preexisting hook") | corpus `RetroactiveLegitimation` + `breach-mistaken-for-authorization`; law: **nunc pro tunc / relation-back** (FRCP 15(c); *Roman Catholic Archdiocese of San Juan v. Acevedo Feliciano*) |
| temporal structure + **no-backdating** | **bitemporal databases** — valid-time vs transaction-time, transaction-time immutable & system-supplied (Jensen/Snodgrass consensus glossary, LNCS 1399) |
| provisional-accept-now-validate-later + active lapse | **provisional ballots** (HAVA, 52 U.S.C. §21082); **OCC** validation phase (Kung–Robinson 1981); lapse = timeout/abort |
| digest-bound grant witnessed-before-reliance, re-fires at renewal | **RFC 3161** trusted timestamping ("a direct recursive application"); **CT / RFC 6962** SCT + MMD = bounded late fulfillment |
| the grant object (digest-bound, attenuated, expiring, quota-scoped capability) | **macaroons** (caveats / attenuation / discharge caveats) — codex surfaced this in search |

**What survives:** only the *phrasing* — "deferral grant as a first-class, digest-bound,
pre-witnessed, expiring, quota-scoped capability." **Substance is occupied.** The assembly
contributes a checklist/state machine, **not a defensible novel prior-art gap.** Fatal reviewer
objection: *obvious aggregation, no new mechanism.*

## What this is good for (cite, don't coin)

A green, executable, zero-axiom **specimen** that makes the matched pair concrete (forbidden vs
lawful backflow) and formalizes the witness-before-reliance recursion. Usable as a worked
example *citing the parts above* — same status as `prediction-markets-case.md`: real artifact,
not a novelty claim. If it ever wants a name, the honest one points outward (macaroon-shaped
deferral capability over a bitemporal/relation-back discipline), not a new kernel coinage.

## Provenance + interferometry note

Shower thought: "anti-causality coefficient acting on truth-as-state." Both non-Claude *and*
Claude passes killed the **coefficient** (graded admissibility is poison — count violations,
never grade) and converged on the **DeferralGrant object**. That three-model convergence was the
**warning light, not the signal** ([[feedback-claude-common-mode-synthesis]]) — I declined to
co-sign as a third Claude and ran the uncorrelated instrument instead. claude-web's one real
contribution was *divergence-not-convergence*: it caught that Chatty's prose had the recursion
and Chatty's Lean didn't enforce it (the dropped `witness_ref ≠ none` hole) — a genuine
formalization catch. Codex (non-Claude, web-searched) then returned **RE-ASSEMBLY** on the whole
machinery. Net: coefficient dead, object green, object occupied. Sibling verdicts:
`distributional-shadow-tombstone.md`, `prediction-markets-case.md`, `predicate-witness-gap.md`.
